/**
 * 
 */
package smf.local.SimpleActivities;

import android.os.Bundle;
import android.preference.PreferenceActivity;
import smf.local.R;

/**
 * @author Vitaly.Chupaev
 *
 */
public class PreferencesList extends PreferenceActivity 
{
	@Override
	protected void onCreate(Bundle savedInstanceState) 
	{
		super.onCreate(savedInstanceState);
		addPreferencesFromResource(R.xml.preference);
	}

    /*
    @Override
    public boolean onCreateOptionsMenu(Menu menu) 
    {
    	try
    	{
	        MenuInflater inflater = getMenuInflater();
	        inflater.inflate(R.menu.pref_menu, menu);
    	}
		catch (Exception ex)
		{
			ErrorHelper.ShowError(ex);
		}
    	
	    return true;
    }
    
    @Override
    public boolean onOptionsItemSelected(MenuItem item) 
    {
    	try 
    	{
	        // Handle item selection
	        switch (item.getItemId()) 
	        {
                case R.id.toHome:
                    Intent sIntent = new Intent(this, Statistics.class);
                    startActivity(sIntent);
                    break;
                case R.id.toContacts:
		            Intent cIntent = new Intent(this, RuleList.class);
		            startActivity(cIntent);
		        	break;
		        case R.id.toFilters:
		            Intent fIntent = new Intent(this, TracesList.class);
		            startActivity(fIntent);
		        	break;
		        case R.id.toMessages:
		            Intent mIntent = new Intent(this, BlockedMessagesList.class);
		            startActivity(mIntent);
		        	break;
	            default:
	                return super.onOptionsItemSelected(item);
	        }
    	}
		catch (Exception ex)
		{
			ErrorHelper.ShowError(ex);
		}
    	return true;
    }   */
}
